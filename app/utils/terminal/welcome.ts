/**
 * Single source of truth for the terminal's welcome banner — shown on
 * mount and restored by `clear` so the terminal never looks fully blank.
 */
export const WELCOME_LINES = [
  'Bienvenido a la terminal del portfolio.',
  "Escribe 'help' para ver los comandos disponibles."
]
